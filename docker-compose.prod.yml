# Environnement de production : image autoportante (nginx + php-fpm + assets
# Vite déjà buildés), avec worker de queue et scheduler séparés.
#
# L'image est construite par GitHub Actions et publiée sur GHCR — le VPS ne fait
# que la tirer. Il n'a que ~2,7 Gi de marge mémoire et héberge déjà deux sites en
# production : un build de Vite sur place risquerait de déclencher l'OOM killer
# sur les conteneurs voisins.
#
#   VPS   : docker compose -f docker-compose.prod.yml pull app && up -d
#   Local : docker compose -f docker-compose.prod.yml up -d --build
#
# IVOIR_IMAGE permet d'épingler un SHA précis pour revenir en arrière :
#   IVOIR_IMAGE=ghcr.io/shikileliondor/ivoir-app:<sha> docker compose ... up -d
services:
    app:
        build:
            context: '.'
            dockerfile: 'docker/app/Dockerfile'
        image: '${IVOIR_IMAGE:-ghcr.io/shikileliondor/ivoir-app:latest}'
        ports:
            - '${APP_PROD_PORT:-8086}:80'
        volumes:
            - './.env:/var/www/html/.env:ro'
            - './storage:/var/www/html/storage'
        networks:
            - ivoir
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
        restart: unless-stopped

    queue:
        image: '${IVOIR_IMAGE:-ghcr.io/shikileliondor/ivoir-app:latest}'
        command: ['php', '/var/www/html/artisan', 'queue:work', '--sleep=3', '--tries=3', '--max-time=3600']
        volumes:
            - './.env:/var/www/html/.env:ro'
            - './storage:/var/www/html/storage'
        networks:
            - ivoir
        depends_on:
            - app
        restart: unless-stopped

    scheduler:
        image: '${IVOIR_IMAGE:-ghcr.io/shikileliondor/ivoir-app:latest}'
        command: ['/bin/sh', '-c', 'while true; do php /var/www/html/artisan schedule:run --verbose --no-interaction; sleep 60; done']
        volumes:
            - './.env:/var/www/html/.env:ro'
            - './storage:/var/www/html/storage'
        networks:
            - ivoir
        depends_on:
            - app
        restart: unless-stopped

    mysql:
        image: 'mysql:8.4'
        environment:
            MYSQL_ROOT_PASSWORD: '${DB_PASSWORD}'
            MYSQL_ROOT_HOST: '%'
            MYSQL_DATABASE: '${DB_DATABASE}'
            MYSQL_USER: '${DB_USERNAME}'
            MYSQL_PASSWORD: '${DB_PASSWORD}'
        volumes:
            - 'ivoir-mysql:/var/lib/mysql'
        networks:
            - ivoir
        healthcheck:
            test:
                - CMD
                - mysqladmin
                - ping
                - '-p${DB_PASSWORD}'
            retries: 3
            timeout: 5s
        restart: unless-stopped

    redis:
        image: 'redis:alpine'
        volumes:
            - 'ivoir-redis:/data'
        networks:
            - ivoir
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s
        restart: unless-stopped

networks:
    ivoir:
        driver: bridge

volumes:
    ivoir-mysql:
        driver: local
    ivoir-redis:
        driver: local
